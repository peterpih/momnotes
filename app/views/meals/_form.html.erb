<%= form_for(meal) do |f| %>
  <% if meal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meal.errors.count, "error") %> prohibited this meal from being saved:</h2>

      <ul>
      <% meal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
    <%= f.label :date %>
    <%= f.datetime_select :date %>
  </div>
 <!--  <div class="field">
    <%= f.label :name %>
    <%= f.select :name, MealType.all.collect { |m| [m.name, m.name] } %>
  </div> -->
  <div class="field">
    <%= f.label :name %>
    <%= collection_select(:meal, :name, MealType.all, :name, :name, {}, {}) %>
  </div>
  <div class='field'>
    <%= f.collection_check_boxes(:meal_type, MealType.all, :id, :name) do |t| %>
      <%= t.name %>
      <%= t.check_box + t.text %>
    <% end %>
  </div>
<!--   <div class='field'>
    <% Food.all.each do |food| %>
      <li>
        <%= check_box_tag 'food_ids[]', food.id -%>
        <%= h food.name -%>
      </li>
    <% end %>
  </div> -->

  <div class="actions">
    <%= f.submit  class: 'btn btn-success btn-md' %>
  </div>
<% end %>
