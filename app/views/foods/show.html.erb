<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @food.name %>
</p>

<p>

<%= link_to 'Edit', edit_food_path(@food), class: 'btn btn-info btn-xs' %>
<%= link_to 'Back to Foods List', foods_path, class: 'btn btn-danger btn-xs' %>
